ARG LLM folder

This folder will contain examples, model artifacts, and utilities for managing a local LLM.

Structure ideas:

- examples/   -> user-provided example reviews (plain text or JSON)
- models/     -> local model files and checkpoints
- training/   -> scripts to run local fine-tuning workflows

For now this is documentation and placeholder; actual implementation depends on chosen local LLM stack.
